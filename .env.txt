# 监听端口
PORT=3000

# MySQL 数据库配置
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=yourpassword    # ← 修改为你的 MySQL 密码
MYSQL_DATABASE=timesnap        # ← 数据库名（提前创建好）

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
